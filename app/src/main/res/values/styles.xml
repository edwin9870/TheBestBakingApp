<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorSecondary</item>
    </style>

    <style name="headLine" parent="@style/Base.TextAppearance.AppCompat.Headline">
        <item name="android:textColor">@color/colorSecondary</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="recipeItem" parent="@style/Base.TextAppearance.AppCompat.Title">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">@dimen/text_recipe_step_short_description_size</item>
        <item name="android:textColor">@color/colorSecondary</item>
    </style>

    <style name="ingredient">
        <item name="android:textColor">@color/colorSecondaryDark</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/text_recipe_step_short_description_size</item>
        <item name="android:layout_width">wrap_content</item>
    </style>


</resources>
